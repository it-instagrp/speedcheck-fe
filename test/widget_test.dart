import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('SpeedCheck compile and smoke test', (WidgetTester tester) async {
    // Verify compile succeeds
    expect(true, isTrue);
  });
}
